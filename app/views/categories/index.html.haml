%h1.center Listing all Categories
= will_paginate
.well.center.col-md-4.col-md-offset-4
  - @categories.each do |category|
    %h3
      = link_to category.name, category_path(category)
= will_paginate